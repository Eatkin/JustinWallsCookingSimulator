/// @description

if (!instance_exists(obj_fadeOut) and !instance_exists(obj_fadeIn) and !instance_exists(obj_justinCrying))
	event_user(0);
	
//This makes sure you can't click something twice on the same frame
//mouse_clear(mb_left);